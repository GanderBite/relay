{
  "name": "hello-world",
  "version": "0.1.0",
  "description": "The smallest valid Relay race. Two prompt runners that produce a markdown greeting.",
  "private": true,
  "type": "module",
  "main": "./dist/race.js",
  "files": ["dist", "prompts", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "relay test ."
  },
  "peerDependencies": {
    "@relay/core": "^1.0.0"
  },
  "devDependencies": {
    "@relay/core": "workspace:*",
    "@types/node": "25.6.0",
    "typescript": "6.0.3"
  },
  "relay": {
    "displayName": "Hello World",
    "tags": ["example", "tutorial"],
    "estimatedCostUsd": { "min": 0.00, "max": 0.02 },
    "estimatedDurationMin": { "min": 1, "max": 3 },
    "audience": ["dev"]
  }
}
